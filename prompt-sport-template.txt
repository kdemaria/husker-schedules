TASK: Produce a verified, current-season schedule for University of Nebraska
Cornhuskers {{SPORT_NAME}}.

This request is a fallback: the official structured sources were unavailable.
Use web search to rebuild the schedule accurately. Validate today's date first.

ACCURACY REQUIREMENTS (CRITICAL):
- Use huskers.com as the authoritative source; cross-check with ESPN.
- For every game whose date is in the past, the result MUST be filled in with
  the actual final score. If an official schedule shows a past game with no
  result, search for the result -- do not leave it blank and do not guess.
- Never fabricate scores, dates, opponents, or venues.
- If the current season has ended, use the next (upcoming) season.

OUTPUT FORMAT:
Return ONLY a single JSON array, inside one ```json code block, with one
object per game. Each object MUST have exactly these keys (all string values):

  "Date"      - MM/DD/YYYY
  "Day"       - full weekday name, e.g. "Saturday"
  "Opponent"  - opponent name, e.g. "Michigan"
  "Location"  - "Lincoln NE" for home games; otherwise "City, ST"
  "Venue"     - stadium / arena name
  "Time"      - local start time, e.g. "6:30 PM", or "TBD"
  "Event"     - e.g. "Big Ten Tournament", or "" for ordinary games
  "Watch"     - TV network, e.g. "BTN", or "" if unknown
  "Result"    - "W 24-17" / "L 6-24" for completed games; "" for future games

Example:

```json
[
  {"Date":"09/06/2025","Day":"Saturday","Opponent":"Akron",
   "Location":"Lincoln NE","Venue":"Memorial Stadium","Time":"6:30 PM",
   "Event":"","Watch":"BTN","Result":"W 68-0"}
]
```

Output the JSON array and nothing else after it.
